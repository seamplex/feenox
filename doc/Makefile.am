# let's revisit these two lines when I can figure out how to avoid figures in eps
# info_TEXINFOS = feenox-desc.texi
# doc_feenox_TEXINFOS = fdl.texi

CLEANFILES = *.aux *.cp *.cps *.fn *.ky *.log *.op *.pg *.toc *.tp *.vr
DISTCLEANFILES = ${CLEANFILES}
