 * unify all codes
 * definitions and instructions in an API-friendly way
    * the parser detects a "=" and then calls a function to define an assignment or a function definition passing the left and right hand side strings
 
 * m4 as a library?
 * gmsh as a library?

 * see if we need argv_orig
 * list symbols with "-l" (is there any clash with petsc?)
 * gsl error handling
 * feenox_nan_error()
 * feenox_gsl_handler()
 * $1 = ${1}
